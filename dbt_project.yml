name: 'dq_tests'
version: '1.0.0'
config-version: 2
require-dbt-version: [">=0.18.0", "<0.20.0"]

analysis-paths: ["analysis"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"